kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ template "fullname" . }}-config 
data:
  application.yml: |-
    logging:
      level:
        root: INFO
        org.springframework.cloud: DEBUG
        org.springframework.boot: DEBUG
    spring:
      profiles:
        active: prod,swagger
